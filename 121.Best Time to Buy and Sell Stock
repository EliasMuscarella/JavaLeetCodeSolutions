class Solution {
    public int maxProfit(int[] prices) {
        int result = 0;
        int min = prices[0];

        for(int price : prices){
            if(price < min){
                min = price;
            }

            result = Math.max(result, price - min);
        }

        return result;
    }
}
